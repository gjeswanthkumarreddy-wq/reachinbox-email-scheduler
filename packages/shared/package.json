{
  "name": "@reachinbox/shared",
  "version": "1.0.0",
  "description": "Shared TypeScript types and constants for ReachInbox",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "*",
    "rimraf": "^5.0.0",
    "@types/node": "^20.12.12"
  }
}